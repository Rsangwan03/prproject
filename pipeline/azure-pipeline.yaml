trigger:
  branches:
    include:
      - main

pool: self-hosted-pool

variables:
  workingDir: 'module'  # make sure this folder exists in repo

stages:
- stage: initAndPlan
  displayName: Init and Plan
  jobs:
  - job: terraformInit
    displayName: Initialization
    steps:
    - task: TerraformInstaller@1
      inputs:
        terraformVersion: '1.14.4'  # or 'latest'
    - task: TerraformTaskV4@4
      displayName: 'Terraform Init'
      inputs:
        provider: 'azurerm'
        command: 'init'
        workingDirectory: '$(workingDir)'
        backendServiceArm: 'pr-connection'
        backendAzureRmResourceGroupName: 'pr-store-group'
        backendAzureRmStorageAccountName: 'prstorageaccount01'
        backendAzureRmContainerName: 'statefile'
        backendAzureRmKey: 'pr.statefile'
    - task: TerraformTaskV4@4
      displayName: 'Terraform Plan'
      inputs:
        provider: 'azurerm'
        command: 'plan'
        workingDirectory: '$(workingDir)'
        environmentServiceNameAzureRM: 'pr-connection'
    - task: TerraformTaskV4@4
      displayName: 'Terraform Apply'
      inputs:
        provider: 'azurerm'
        command: 'apply'
        workingDirectory: '$(workingDir)'
        commandOptions: '--auto-approve'
        environmentServiceNameAzureRM: 'pr-connection'
